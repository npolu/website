// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // ⬇️ After you deploy, set this to your live URL (used for SEO + sitemaps).
  // e.g. 'https://npolu.netlify.app' or your custom domain.
  site: 'https://example.com',
});
